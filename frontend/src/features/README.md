# Feature modules

Each product area owns its feature-specific components, hooks, schemas, queries,
and types here. Shared, feature-agnostic code remains in `components`, `hooks`,
`constants`, `lib`, or `utils`.

Authentication, dashboard, and AI chat now own their feature-specific modules.
Remaining feature boundaries stay intentionally minimal until their dedicated
implementation work begins.
